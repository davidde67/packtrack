# 2026-03-31

## Morning Brief - Trash Day Tracking Added

- David asked to add trash day as a data point to every housesit in the Pet Sitting spreadsheet
- Added "Trash Day" column (P) to Pet Sitting Google Sheet
- **10 Margaret Ave (Diana & Bill's):** Friday — filled in row 26
- **391 Lakeshore Dr:** Tuesday (confirmed by David — "today is trash day" on Tue 3/31)
  - Note: David said "Shoreline" instead of "Lakeshore" — seems to be a verbal tic
  - Based on SW zone (Excelsior/Ingleside area where 94112 is), 10 Margaret is Friday
  - 391 Lakeshore Dr (94132) confirmed Tuesday
- David wants trash day in EVERY morning brief going forward
- David called Fritz "right hand and right prefrontal cortex" — affirming, nice

## cPanel / FTP Credentials (updated 2026-03-31)
- **cPanel:** https://smartconfigurations.com:2083 — user: `fritz`, pass: `ThisIsFritzLangosta`
- **phpMyAdmin:** same URL, same credentials
- **FTP/SFTP:** host `ftp.smartconfigurations.com` (or 199.188.200.138 port 22 blocked), user `fritz@smartconfigurations.com`, pass `ThisIsFritzLangosta`
- FTP root: `/home/smartbvbv/` — web root is `public_html/`
- **Document root for smartconfigurations.com:** NOT /public_html/ — domain shows "Namecheap Parking Page" at root, but Nextcloud works at /nextcloud/ — actual web root is managed by LiteSpeed differently than FTP path
- **Photo upload fix (2026-03-31):** LiteSpeed PHP can't reach Nextcloud WebDAV at `localhost` or `127.0.0.1`. Fixed by replacing PHP proxy upload with direct browser→Nextcloud WebDAV PUT (CORS-friendly). Updated packtrack.html on Nextcloud: now uploads directly via `fetch(WEBDAV_BASE + '/' + id + '.jpg', {method:'PUT'})` instead of `upload.php`. upload.php left on server but no longer used.

## GitHub Repo Launched — PackTrack
- Repo: https://github.com/davidde67/packtrack
- Pushed: v1.01 tag, full README, demo.html
- Cleaned workspace: all OpenClaw internals (.md files, memory/, skills/, credentials) REMOVED from git history (force-pushed)
- **GitHub auth:** davidde67 account, HTTPS, working
- **Commit attribution issue:** commits show as "Langosta" (no GitHub user) — git user.name/email not configured on this machine pre-first-commit. Fix going forward by setting git config.

## Demo Site — Netlify Git-Based Deploy
David wants to learn Git-based deploy. Plan:
1. Netlify connects to GitHub repo (davidde67/packtrack)
2. No build command needed (pure HTML/JS)
3. Publish directory: `/`
4. Auto-deploys on every push to main
5. Custom domain: packtrack.smartconfigurations.com or similar
6. David needs to do the Netlify UI step (browser auth required)
7. README updated with step-by-step Netlify deploy instructions

**Demo URL (pending Netlify setup):** https://packtrack-demo.netlify.app
**Demo PIN:** 000000
**Demo data:** 15 fake items pre-loaded (MacBook, camera, umbrella, passport, etc.)

## April Trip Prep
- Still pending (April 15-25ish)
